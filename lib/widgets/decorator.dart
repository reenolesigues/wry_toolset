import 'package:flutter/material.dart';

Container wryContainer() {
  Container c = new Container(
    decoration: BoxDecoration(
      borderRadius: BorderRadius.all(Radius.circular(10)),
    ),
  );
  return c;
}